<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LRDXRClient"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|uiMode|density|keyboardHidden"
            android:resizeableActivity="true"
            android:theme="@style/Theme.LRDXRClient">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Quest specific intent filter for VR -->
            <meta-data android:name="com.oculus.vr.focusaware" android:value="true"/>
            <meta-data android:name="com.oculus.supportedDevices" android:value="quest|quest2|quest3|questpro"/>
            
            <!-- Quest 2D Window Configuration -->
            <meta-data android:name="com.oculus.vrshell.desktop_mode_dims" android:value="1920x1080"/>
            <meta-data android:name="com.oculus.vrshell.desktop_dpi" android:value="240"/>
            
            <layout
                android:defaultHeight="1080dp"
                android:defaultWidth="1920dp"
                android:gravity="center"
                android:minHeight="600dp"
                android:minWidth="800dp" />
            
            <!-- Quest 2D Window Configuration -->
            <!-- Suggest a larger default window size (1920x1080) -->
            <meta-data android:name="com.oculus.vrshell.desktop_mode_dims" android:value="1920x1080"/>
            <!-- Increase density to avoid "crushed" UI and low-res rendering (default is often 160 or similar) -->
            <meta-data android:name="com.oculus.vrshell.desktop_dpi" android:value="240"/>
            
            <!-- Standard Android Multi-Window Layout -->
            <layout
                android:defaultHeight="1080dp"
                android:defaultWidth="1920dp"
                android:gravity="center"
                android:minHeight="600dp"
                android:minWidth="800dp" />
        </activity>
    </application>

</manifest>